# ========================================
# Agent-V3 核心依赖
# ========================================

# LangChain核心库
langchain>=0.0.350
langchain-community>=0.0.10
langchain-openai>=0.0.2

# LLM提供商
# OpenAI
openai>=1.3.0

# Anthropic Claude
anthropic>=0.7.0

# Hugging Face
huggingface_hub>=0.19.0
transformers>=4.35.0

# SiliconFlow (通过 OpenAI 兼容 API)
# 无需额外依赖，使用 langchain-openai

# 工具依赖
requests>=2.31.0
duckduckgo-search>=3.9.0

# 配置文件处理
PyYAML>=6.0.1
python-dotenv>=1.0.0  # 环境变量管理

# 数据处理和验证
pydantic>=2.5.0

# Redis缓存和会话管理
redis>=5.0.0

# CrewAI 多智能体框架
crewai>=0.1.0
crewai-tools>=0.1.0

# n8n 工作流集成
# 通过 requests 调用 n8n API，无需额外依赖

# FastAPI Web 框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0  # ASGI 服务器
python-multipart>=0.0.6  # 文件上传支持
websockets>=12.0  # WebSocket 支持

# 向量数据库
chromadb>=0.4.0  # 轻量级向量数据库
# faiss-cpu>=1.7.4  # FAISS (可选)
# pinecone-client>=2.2.0  # Pinecone (可选)

# 文档处理
PyPDF2>=3.0.0  # PDF 处理
python-docx>=0.8.11  # Word 文档处理
openpyxl>=3.1.2  # Excel 处理
pillow>=10.0.0  # 图片处理

# 文本处理
tiktoken>=0.5.0  # Token 计数

# 日志和监控（可选）
# prometheus-client>=0.19.0  # Prometheus 监控
# opentelemetry-api>=1.20.0  # 分布式追踪

# ========================================
# 开发和测试依赖（生产环境可选）
# ========================================
# pytest>=7.4.0
# pytest-asyncio>=0.21.0
# black>=23.0.0
# flake8>=6.0.0
# mypy>=1.7.0